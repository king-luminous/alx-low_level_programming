#include <stdio.h>
#include <time.h>
#include <stdlib.h>
/**
 * main - entry point
 * Return: always success
 */

int main(void)
{
	int d;
	char sixteen;

	for (d = '0'; d <= '9'; d++)
		putchar(d);
	for (sixteen = 'a'; sixteen <= 'f'; sixteen++)
		putchar(sixten);
	putchar('\n');

	return (0);
}
